import { toast } from "react-toastify"

function Boards() {

  return <>egg</>
// (
//     <>
//       {loading ? <p>Loading...</p> 
//       :
//       <>{boards.map((board, id) => (
//         <p key={board.id}>{board.data.name}</p>
//       ))}</>}
//     </>
// )
  }

export default Boards